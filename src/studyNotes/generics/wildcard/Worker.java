package studyNotes.generics.wildcard;

import studyNotes.collections.setAndHashSet.PersonWithHashcode;

public class Worker extends PersonWithHashcode {

    public Worker(String name, int age, String city) {
        super(name, age, city);
    }
}
